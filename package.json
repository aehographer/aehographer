{
  "name": "aeho",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "node --env-file=.env scripts/sync-notion.mjs",
    "sync:grapher": "node --env-file=.env scripts/sync-grapher.mjs",
    "import:grapher": "node --env-file=.env scripts/import-to-notion.mjs",
    "publish": "npm run sync && git add src/content/aeho && git commit -m 'Sync from Notion' && git push",
    "publish:grapher": "npm run sync:grapher && git add src/content/grapher && git commit -m 'Sync grapher from Notion' && git push"
  },
  "dependencies": {
    "astro": "^6.1.5"
  },
  "devDependencies": {
    "@notionhq/client": "^2.3.0",
    "notion-to-md": "^3.1.9"
  }
}
